apiVersion: policy.sigstore.dev/v1beta1
kind: ClusterImagePolicy
metadata:
  name: cloudflared-image-policy
spec:
  match:
    - group: apps
      version: v1
      resource: deployments
      selector:
        matchLabels:
          app: cloudflared
  images:
    - glob: "index.docker.io/cloudflare/cloudflared**"
  authorities:
    - name: allow-all
      static:
        action: pass
  mode: enforce
