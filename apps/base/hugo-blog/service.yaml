apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: blog-uclab8
    app.kubernetes.io/name: nginx
  annotations:
    external-dns.alpha.kubernetes.io/hostname: blog.uclab8.net
  name: blog-server
  namespace: blog
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: blog-uclab8
    app.kubernetes.io/name: nginx
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
