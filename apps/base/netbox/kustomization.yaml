apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: netbox
resources:
  - namespace.yaml
  - secrets.yaml
  - helmrepo.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: netbox-values
    files:
      - values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml
