apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-runner-secret-staging
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend-global
    kind: ClusterSecretStore
  target:
    name: forgejo-runner-secret-staging
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: RUNNER_SECRET
      remoteRef:
        key: forgejo-runner-secret-staging
        property: RUNNER_SECRET
