apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: netbox
  namespace: netbox
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production # Specify the name of your issuer resource
spec:
  gatewayClassName: cilium
  listeners:
    - hostname: netbox.uclab.dev # Specify a valid domain to reach your application
      name: netbox-uclab-dev-http
      port: 80
      protocol: HTTP
    - hostname: netbox.uclab.dev # Specify a valid domain to reach your application
      name: netbox-uclab-dev-https
      port: 443
      protocol: HTTPS
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: netbox-uclab-tls # Specify the secret name to be used with your application
      allowedRoutes:
        namespaces:
          from: All
